# Use a published immutable Aegis release, for example 1.0.0.
AEGIS_VERSION=1.0.0
# Generate with: node -e "console.log(require('node:crypto').randomBytes(32).toString('base64'))"
MASTER_KEY=
# Use a unique random password; do not reuse the development default.
POSTGRES_PASSWORD=
