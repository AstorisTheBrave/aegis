{
  "name": "@aegis/action-engine",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "lint": "prettier --check . --ignore-path ../../.prettierignore",
    "test": "vitest run",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@aegis/action-contract": "workspace:*",
    "@open-saas-governance/audit-ledger": "workspace:*"
  },
  "devDependencies": {
    "@types/node": "24.1.0",
    "typescript": "7.0.2",
    "vitest": "4.1.10"
  }
}
