# Aegis Connector SDK for C

Our C11 SDK exposes a stable, allocation-free ABI for v1 manifest validation
and checkpoint handling. We keep it independent of generated Protobuf/gRPC
code; use a separately versioned language binding for transport.
