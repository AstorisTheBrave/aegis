# Aegis Connector SDK for C

The C11 SDK exposes a stable, allocation-free ABI for v1 manifest validation
and checkpoint handling. It is intentionally independent of generated
Protobuf/gRPC code; use a separately versioned language binding for transport.
